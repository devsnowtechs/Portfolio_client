# Cloudflare reads this file as plain text, so keep it at Unix line endings in every checkout: a CRLF
# would leave a rule unreadable if one is ever needed again. It carries comments only on purpose; the SPA
# fallback lives in wrangler.jsonc (see the file and README.md).
public/_redirects text eol=lf
